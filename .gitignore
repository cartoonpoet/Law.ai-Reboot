.DS_Store
node_modules
.turbo
*.log
dist
dist-ssr
*.local
.env
.cache
server/dist
public/dist

# env
.env.*
!.env.example
!deploy/.env.prod.example

# 배포 런타임 데이터(서버에서 생성: TLS 인증서/ACME 챌린지)
deploy/certbot/

# build
build

# prisma
**/generated
**/*.db

# misc
Law.ai Reboot/

# MCP 설정 (ERDIFY_API_KEY 등 비밀값 포함 — 커밋 금지)
.mcp.json

# superpowers brainstorm (local visual companion)
.superpowers/
